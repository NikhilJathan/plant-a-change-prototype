document.getElementById("search-button").addEventListener("click", function() {
    document.getElementById('openai').innerHTML = '<div class="card"><div class="card-body">This feature is currently unavailable, due to technical limitations with OpenAI, where we need to set up extra servers to ensure the security of the OpenAI key.</div></div>';
  });